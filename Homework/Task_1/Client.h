#pragma once
#include "ATM.h"
#include <string>

using namespace  std;

class Client
{
private:
	string name;

public:
	Client(string name = "Client") : name(name) {}

	void UseATM(ATM* atm)
	{
		while()
	}
};

